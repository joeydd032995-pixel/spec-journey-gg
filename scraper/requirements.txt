fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
apscheduler==3.11.0
pydantic==2.10.4
# Optional resilience fallback (rediscovery if the site changes). The live path
# does not need a browser; install with `playwright install chromium` to enable.
playwright==1.49.1
# Test-only
pytest==8.3.4
